set(CLAMP_CAST_VERSION "@CLAMP_CAST_VERSION@")

@PACKAGE_INIT@

set_and_check(CLAMP_CAST_INCLUDE_DIRS "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")

if (NOT CMAKE_VERSION VERSION_LESS "3.10")
	include("${CMAKE_CURRENT_LIST_DIR}/clamp_castTargets.cmake")
endif()
